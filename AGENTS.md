# 项目协作规则

- 除非我主动要求，否则默认不要进行深度测试。
- 除非我主动要求，否则默认不要进行视觉化对比。
- 除非我主动要求，否则默认不要使用 superpowers。
- 当任务包含两个或以上可以独立推进的子任务时，主动启动子 Agent 并行处理，无需等待我明确要求；简单任务、强耦合任务或可能产生文件写入冲突的任务不要拆分。
- 当我说“把代码提了”时，默认执行完整发布流程：提交当前任务改动、合并到 `main`，并将 `main` 推送到远端。
